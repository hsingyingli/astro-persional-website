---
import type { Career } from "../../../utils/types";
import CareerItem from "./CareerItem.astro";
interface Props {
  careers: Array<Career>;
}

const { careers } = Astro.props;
---

<div class="grid grid-cols-[56px_1fr] gap-4">
  {careers.map((career) => <CareerItem career={career} />)}
</div>
