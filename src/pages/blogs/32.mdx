---
layout: ../../layouts/BlogLayout.astro 
src: /leetcode.jpeg
title: 200. Number of Islands
category: Leetcode 
date: 2022-10-29 04:13:14.841+00 
---

# Description
***Medium***
```
Given an m x n 2D binary grid grid which represents a map of '1's (land) and '0's (water), return the number of islands.

An island is surrounded by water and is formed by connecting adjacent lands horizontally or vertically. You may assume all four edges of the grid are all surrounded by water.
```

# TypeScript
```typescript
function numIslands(grid: string[][]): number {
    let count: number = 0
    for (let row: number = 0 ; row < grid.length ; row++) {
        for (let col: number = 0 ; col < grid[0].length ; col++) {
            if (grid[row][col] === '1') {
                count++
                dfs(grid, row, col)
            }
        }
    }
    return count
};

function dfs(grid: string[][], row: number, col: number) {
    if (grid[row][col] === '1') {
        grid[row][col] = '0'
        if (col > 0) dfs(grid, row, col-1)
        if (col < grid[0].length - 1) dfs(grid, row, col+1)
        if (row > 0) dfs(grid, row-1, col)
        if (row < grid.length - 1) dfs(grid, row+1, col)
    }
}
```

# Python3
```python
class Solution:
    def numIslands(self, grid: List[List[str]]) -> int:
        count = 0
        for row in range(len(grid)):
            for col in range(len(grid[0])):
                if (grid[row][col] == '1'):
                    self.dfs(grid, row, col)
                    count += 1
        return count
    def dfs(self, grid, row, col):
        if (grid[row][col] == '1'):
            grid[row][col] = '0'
            if (col > 0): self.dfs(grid, row, col-1)
            if (col < len(grid[0]) - 1): self.dfs(grid, row, col+1)
            if (row > 0): self.dfs(grid, row-1, col)
            if (row < len(grid) - 1): self.dfs(grid, row+1, col)
```

# Golang
```go
func numIslands(grid [][]byte) int {
    count := 0
    for row := 0 ; row < len(grid) ; row++ {
        for col := 0 ; col < len(grid[0]) ; col++ {
            if grid[row][col] == '1' {
                grid = dfs(grid, row, col)
                count++
            }
        }
    }
    return count
}

func dfs(grid [][]byte, row int, col int) [][]byte {
    if grid[row][col] == '1' {
        grid[row][col] = '0' 
        if col > 0 {
            grid = dfs(grid, row, col-1)
        }

        if col < len(grid[0]) - 1  {
            grid = dfs(grid, row, col+1)
        }

        if row > 0 {
            grid = dfs(grid, row-1, col)
        }

        if row < len(grid) - 1 {
            grid = dfs(grid, row+1, col)
        }
    }
    return grid
}

```
